# wax-sample-portfolio
a partially complete example of using Wax for a portfolio website

to run: download the repository, open terminal, `-cd my/file/path/here` into the directory, and `bundle exec jekyll serve` :)
